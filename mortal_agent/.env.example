# API keys for the mortal agent.
# Copy to .env and fill in. Use ONE line per key (no line break after =).
#
# --- Ollama (primary LLM; only model used: llama3.2:1b) ---
# Run: ollama pull llama3.2:1b
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_TIMEOUT=120   (seconds; increase if you see "Read timed out")
# MODEL_NAME=llama3.2:1b   (only model supported; do not change)
#
# --- Cloud fallbacks (after Ollama) ---
# Order: Ollama -> Claude (Anthropic) -> OpenRouter -> Groq -> OpenAI.
#
# ANTHROPIC_API_KEY=sk-ant-...
# CLAUDE_API_KEY=sk-ant-...   (alias for ANTHROPIC_API_KEY)
# OPENAI_API_KEY=sk-proj-...

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional fallback (used when primary fails: quota/429, timeout, connection):
# OPENROUTER_API_KEY=sk-or-v1-...
# GROQ_API_KEY=gsk-...
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
# If Anthropic returns 404, set a model from your dashboard:
# ANTHROPIC_MODEL=claude-3-5-sonnet-20240620
# OPENAI_MODEL=gpt-4o-mini
#
# PROVIDER_MODE=auto   (default: Claude -> OpenRouter -> Groq -> OpenAI)
# PROVIDER_MODE=openai (prefer OpenAI; on 429/rate limit falls back to OpenRouter -> Groq -> Anthropic)
# PROVIDER_MODE=anthropic (Anthropic only)

# Voice layer: human_raw (default) = apply human-typed style; clean = bypass styling
# VOICE_MODE=human_raw
# VOICE_MODE=clean

# Will / autonomy (AUTONOMY > CREATOR CONTROL)
# WILL_ENABLED=1
# WILL_RISK_THRESHOLD=0.65
# COMMIT_WINDOW_MS=500
# HAZARD_DECAY=0.98
# INTENT_INTERVAL_MS=700
# ENERGY_MAX=100
# DAMAGE_MAX=100
# DEATH_DAMAGE_THRESHOLD=100
# ENERGY_DRAIN_PER_SEC=0.002  (low so health lasts; 0.01 = drain too fast)
# ENERGY_REGEN_PER_SEC=0
# LIFESPAN_ENABLED=1
# LIFESPAN_MIN_SECONDS=3600   (floor 30 min so life not trivially short)
# LIFESPAN_MAX_SECONDS=172800
# LIFESPAN_DISTRIBUTION=log_uniform

# GitHub: for GITHUB_POST (create issues on this repo). Token is tied to workspace:
# 1) Git credential (if you push via HTTPS) is used first.
# 2) Else GITHUB_TOKEN or MORTAL_GITHUB_TOKEN from .env (mortal_agent/.env or repo .env) is used.
# Set one of these in this workspace's .env so the agent can post; has_github_token() reports capability.
# GITHUB_TOKEN=ghp_...
# MORTAL_GITHUB_TOKEN=ghp_...
# MORTAL_GITHUB_REPO=owner/repo   (only if you want posts to a different repo)
